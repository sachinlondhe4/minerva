%h2 Current Year

%p= " vacation year currently being scheduled is #{@current_year}.to change"

= form_tag current_year_update_path, :method => 'put' do
  = text_field_tag "year", "", :id => "year_field"
  = submit_tag "Submit"

